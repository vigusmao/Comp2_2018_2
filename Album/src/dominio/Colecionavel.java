package dominio;

public interface Colecionavel {

    /** Retorna a posição pretendida do item a ser colecionado. */
    int getPosicao();

    /** Retorna a URL da imagem do item. */
    String getUrlDaImagem();
}
